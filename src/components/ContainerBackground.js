import React from "react";
import styled from "styled-components"

const ContainerBackground = styled`
    background-color : grey;
`

function NameSelection() {
    return (
        <ContainerBackground>
            hi
        </ContainerBackground>
    )
}

export default NameSelection